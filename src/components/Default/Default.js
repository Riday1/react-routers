import React from 'react';
import Header from '../Header/Header';

const Default = () => {
    return (
        <div>
            <Header></Header>
            <h1>From Default Component</h1>
        </div>
    );
};

export default Default;